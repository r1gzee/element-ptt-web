/*
Copyright 2024 New Vector Ltd.

SPDX-License-Identifier: AGPL-3.0-only OR GPL-3.0-only OR LicenseRef-Element-Commercial
Please see LICENSE files in the repository root for full details.
*/

.mx_VoiceChannelPanel {
    display: flex;
    flex-direction: column;
    border-top: 1px solid $separator;
    background-color: $background;
    padding: var(--cpd-space-2x) var(--cpd-space-4x);
    gap: var(--cpd-space-2x);
    flex-shrink: 0;
}

.mx_VoiceChannelPanel_header {
    display: flex;
    align-items: center;
    gap: var(--cpd-space-3x);
    min-height: 32px;
    flex-wrap: wrap;
}

.mx_VoiceChannelPanel_title {
    color: var(--cpd-color-text-action-accent);
    flex: 1;
    display: flex;
    align-items: center;
    gap: var(--cpd-space-1-5x);

    /* Small pulse indicator shown when anyone is in the channel */
    &::before {
        content: "";
        display: inline-block;
        width: 8px;
        height: 8px;
        border-radius: 50%;
        background-color: var(--cpd-color-text-action-accent);
        flex-shrink: 0;
    }
}

.mx_VoiceChannelPanel_participants {
    display: flex;
    align-items: center;
    gap: var(--cpd-space-1-5x);
    color: $secondary-content;
}

.mx_VoiceChannelPanel_controls {
    display: flex;
    align-items: center;
    gap: var(--cpd-space-2x);
}

/* ── Inline mic button + label ─────────────────────────────────── */

.mx_VoiceChannelPanel_micWrapper {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--cpd-space-1x);
}

.mx_VoiceChannelPanel_micLabel {
    color: $secondary-content;
    white-space: nowrap;
}

.mx_VoiceChannelPanel_micButton {
    &.mx_VoiceChannelPanel_micButton_active {
        background-color: var(--cpd-color-bg-action-primary-rest);
        color: var(--cpd-color-icon-on-solid-primary);

        &:hover {
            background-color: var(--cpd-color-bg-action-primary-hovered);
        }
    }

    /* Floor occupied: amber tint instead of plain opacity so it reads as
       "wait" rather than "disabled". */
    &.mx_VoiceChannelPanel_micButton_blocked {
        cursor: not-allowed;
        background-color: hsl(38, 50%, 13%);
        color: hsl(38, 90%, 62%);

        &:hover {
            background-color: hsl(38, 50%, 17%);
        }
    }
}

/* ── Mode row (PTT ↔ Live Audio toggle) ────────────────────────── */

.mx_VoiceChannelPanel_modeRow {
    display: flex;
    justify-content: center;
    padding-bottom: var(--cpd-space-1x);
}

/* ── Collapsed state ─────────────────────────────────────────────── */

.mx_VoiceChannelPanel_collapsed {
    .mx_VoiceChannelPanel_modeRow {
        display: none;
    }
}
